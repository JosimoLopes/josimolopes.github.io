export default function initWorkPicture() {}

// const workList = document.querySelectorAll(".work-container");
// const workImage = document.querySelector(".work-img");

// function handleMouseEnter(e) {
//   if (!workImage.classList.contains("active")) {
//     workImage.classList.add("active");
//     workImage.setAttribute("src", e.currentTarget.dataset.src);
//   }
// }

// function handleMouseMove(e) {
//   const x = e.pageX - scrollX;
//   const y = e.pageY - scrollY;

//   workImage.style.top = y - 150 + "px";
//   workImage.style.left = x - 150 + "px";
// }

// function handleMouseLeave() {
//   workImage.classList.remove("active");
// }

// setTimeout(() => {
//   window.addEventListener("mousemove", handleMouseMove);
// }, 500);

// workList.forEach((item) => {
//   item.addEventListener("mouseover", handleMouseEnter);
//   item.addEventListener("mouseout", handleMouseLeave);
// });
